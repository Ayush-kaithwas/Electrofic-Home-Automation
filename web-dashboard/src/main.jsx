/* -------------------------------------------------------------
 * main.jsx — Entry point for React DOM Client renderer
 * ------------------------------------------------------------- */
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(React.createElement(window.App));
